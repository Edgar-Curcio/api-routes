apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: webapp
  namespace: webapp
spec:
  hosts:
  - webapp.com
  gateways:
  - webapp/webapp-gateway
  http:
  - route:
    - destination:
        host: webapp-chart
        subset: v1
      weight: 100